# dependencies
node_modules
.pnp
.pnp.js
.yarn/install-state.gz

# testing
coverage

# next.js
.next/
out/
build
dist

# production
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem

# Large bonus media — delivered via Google Drive, not served by the site.
# (GitHub rejects files over 100MB; these are ~119MB and ~878MB.)
public/Solani Bonuses/*.mp4
public/Solani Bonuses/*.wav

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — never commit secrets
.env
.env.local
.env*.local

# vercel
.vercel

# IDE
.vscode/
.idea/

# Claude Code — share team setup (settings + skills), keep personal overrides out
.claude/settings.local.json
